//exercicio escreva um programa que lê 10 números inteiros e os salva em um vetor.
//Em seguida, o programa deve encontrar a posição do mair elemento do vetor e imprimir a sua poisção;
#include <stdio.h>
//declarar as variaveis
//pedir para o usuario dar os numeros
//guardar dentro de um vetor
//imprimir a posição do maior deles
int main(void) {
  int maior, n,i, vet[9];

  for(i=0;i<n;i++ ){
    printf("Digite um número: ");
    scanf("%d", &n);
    }
  vet[n] = n;
  printf("%d",vet[n] );
  
  return 0;
  
}